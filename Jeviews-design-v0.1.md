# Jeviews 設計 v0\.1 — 内部ハーネスとレビューの哲学

2026\-09\-18

## 最新の採用方針 — 2026-09-19

新しいチャットでは本節と `Jeviews-project-layout-v0.1.md` を先に読む。ユーザーはFlue＋RouterJev＋JudgeJevを採用して構築を進め、問題があればFlueを外すと決めた。「採用前に比較して決める」という旧方針から変更した。以下は設計への反映であり、FlueとJevだけによる自律進行を動作確認した記録ではない。

### 役割と依存

- 内部でモデルによる判断を行うのはRouterJevとJudgeJev。汎用的な推論・追加調査・修正・教師レビューは、従来どおり外側のサブスクCoding Agentに任せる。
- RouterJevが実在する取得候補から次に読む材料を選び、JudgeJevが問題・根拠不足・追加調査の必要性を評価する。Jevの構造化判断と、実在する候補・根拠との対応を維持する。
- Flueを、取得・判定・追加調査・終了を進めるAgentの実行基盤として使う。レビューの判断基準、状態遷移、上限、公開結果はJeviewsの型と関数で定義し、Flueから利用する。
- Flue向け接続の複雑さ、Stateの二重管理、上限・出力を保てないなどの問題が生じたら、実行部分を通常のTypeScriptへ置き換える。CLI/MCPの契約やProvider Adapterを変更せずに外せる境界にする。
- ディレクトリは役割で命名する。`flue/` や `experiments/flue/` は置かず、`src/runtime/agent.ts` にAgent定義、`src/runtime/run.ts` に起動・実行・終了処理を置く。Flue/Pi依存と必要な形式変換はこの実行層に閉じる。
- `src/review/` はRouter・Judge・確認項目・コンテキスト構成・State・公開結果・必要なI/Fを持つ。`src/adapters/providers/` はTypeSafe直結と各Gatewayへの通信、`src/adapters/repository/` はGitとファイル取得を持つ。
- TypeSafe直結から実装する。Vercel AI Gateway・OpenRouter・Cloudflare対応は、接続先Adapterとして追加する。APIキーは環境変数から起動時に読み、選択した接続先だけに渡す。

### 実装時に最初に確かめること

Flueの標準Agentは `useModel()` が必須である。参考の `flue-jev-demo` はJevをToolとして登録し、別のGPT-4o-miniが進行を担う構成だった。したがって、Jevの判断だけをFlueの継続・取得・終了につなぐ接続はまだ未検証である。

まず「候補選択 → ファイル取得 → 再判定 → 未確認事項を含む結果」をFlueとJevで動かす。内部に汎用LLMを追加して解決したことにはしない。自動圧縮なども含め、意図しないモデル呼び出しが起きないことを確かめる。初期検証でFlueの利点より複雑さが大きいとわかった場合は、ユーザーの方針どおり外す。

参考：[Flue Models](https://flueframework.com/docs/guide/models/)、[flue-jev-demo](https://github.com/matthewp/flue-jev-demo)。Flueで任意のTypeScript処理まで自動的に再開可能になるとは仮定しない。

### CLIと未確認事項

```bash
jeview          # diffが既定
jeview diff     # 未ステージの変更
jeview staged   # ステージ済みの変更
jeview all      # Git追跡ファイル全体
```

`diff` はインデックスと作業ツリー、`staged` はHEADとインデックスの差分を扱う。`staged` の取得には未ステージ編集を混ぜない。`all` はGit追跡ファイルの作業ツリー内容を対象にする。対象の選択とModeは別であり、既定Modeをここでは変更しない。

確認できた問題と未確認事項の両方をCoding Agentへ渡す。1リクエストのコンテキスト上限を踏まえて材料を選ぶことと、調査を有限で終了させることは別の制約として管理する。公開JSONの詳細や集約規則は引き続き未決とする。

評価は開発と並行し、Astra・Fableによる外側の教師レビューと突き合わせる。FlueのEvalsガイドとVitest/vitest-evalsは参考候補であり、具体的な件数・閾値・実行頻度は実装後のユーザー指示で決める。本文の説明モデルや保存方式の詳細は旧案を含むため、内部に汎用LLMを置かない本節の前提を優先して具体化する。

## 再開用メモ — 2026-09-18の壁打ち結果

この節は2026-09-18時点の経緯である。新しいチャットでは冒頭の「最新の採用方針」を先に読む。未解決のレビュー指摘を修正済みとして扱わない。Stateの型、CLIの細かな契約、数値設定は引き続き設計案である。

### 現在地と進め方

- Jeviewsの設計を検討している。この会話で実装コードの変更、Jevによる実レビュー、教師モデルによる評価は行っていない。
- 最優先はJevの特性を生かすこと。まず小さく作って感触をつかみ、必要が見えた機能を段階的に追加する。
- 設計の検討を既存実装の都合に縛らない。既存コードへの接続方法は、実装に入るときの別の確認事項とする。
- Jeviewsの哲学を優先し、SOLIDは開発や変更を助けるために使う。過剰な分割や抽象化を目的にしない。
- 説明には、初見のエンジニアにもわかる平易な言葉を使う。
- ユーザーに判断を求めるときは、不明度の大きい論点から一つずつ聞く。合意済みの方針を毎回聞き直さない。
- OKF形式へのリライト依頼は取り下げ済み。OKF化を再開しない。

### 今回決まった方針

| 論点 | 決定内容 |
| --- | --- |
| 対応言語 | 言語を限定しない。TS・JS限定の初版案を変更する |
| 初版の取得方法 | ファイルの読み取り、文字列検索、ファイル名検索で小さく始める |
| 言語サーバー | 後から任意の拡張機能として使えるようにする。初版でTypeScriptの解析機能を必須にする案は採用しない |
| 取得と判断の分担 | ハーネスが実在する候補と根拠を用意し、Jevが取得先の選択と評価を担当する |
| 初版の観点 | 入力の検証漏れ、エラーの握りつぶし、秘密情報の露出、コードのフォーマット、Lint相当の判断の5つ |
| フォーマットとLint | Jev自身に判断させる。既存Formatter・Linterを実行して結果を取り込む案は採用しない |
| 慣習の推測 | 周囲のコードから書き方の慣習を推測する指示は、初版のプロンプトに含めない。必要になったら検討する |
| 評価の対象 | Jeviewsを開発しながら、このプロジェクト自身に対して評価を回す |
| 評価の教師 | AstraとFableにレビューを依頼し、Jeviewsの結果と突き合わせる |
| 評価の具体化 | 具体的な評価案は、実装が進んだ段階でユーザーが指示する。今は評価セットの件数、閾値、合格率、実行頻度を固定しない |

最初の3観点は39候補の5・7・18に対応する。追加されたフォーマットとLint相当は、対象ルールの詳細や既存の7軸への割り当てをまだ確定していない。39候補の番号は選定履歴として維持する。
インデント、引用符、未使用変数、重複条件は会話中の例であり、すべてを初版の必須ルールとして採用したわけではない。自動修正も決定していない。

### 継続する基本方針

- Jevを判断の中心に置き、CLIとJSONを人間・Git Hooks・CI・Agentから呼べるようにする。将来はCLIと `SKILL.md` を提供する。
- 7つの哲学を内部ハーネスに持たせる。哲学は判断の軸、Modeは調査と説明の深さを表す。
- 追加調査はリポジトリ内に閉じる。外部のWeb検索や資料取得は行わない。
- 追加調査の必要性はJevに判断させ、ハーネスが有限の回数上限を守る。上限の具体値は未確定。
- 根拠が不足する場合は `NEED_REVIEW` とし、不明事項を残す。軸別の点数を平均せず、対象外を `GOOD` に数えない。
- 一つの対象が失敗しても、その失敗を記録して残りは続行する。
- 共通の処理で `scan`・`investigate`・`explain` を扱う。コアはFlueに依存させず、通常のTypeScript関数とデータで構成する。2026-09-19の変更により実行層にはFlueを採用する。
- RouterとJudgeは役割の分担であり、哲学ごとの別エージェントを作ることを意味しない。実行時のStateと公開結果は別の型にする。

### 取得手段のたたき台

小さな初版を前提に、会話では次の5種類を提案した。種類ごとの検索規則、取得範囲、候補の上限、候補IDの作り方は未確定である。

| 読むもの | 初版の探し方の案 |
| --- | --- |
| 対象コードの前後 | 同じファイルの必要範囲を読む |
| 関連するコード | コード中に実在する名前や文字列、ファイル名を検索する。検索ヒットを確定した呼び出し関係として扱わない |
| 関連するテスト | ファイル名や参照文字列から候補を探す |
| 仕様・開発方針 | リポジトリ内のREADMEなどの資料を探す |
| 設定ファイル | リポジトリ内の設定を読む。検査コマンドの実行はこの取得方法に含めない |

Jevに渡す問いと材料は小さくする。同じ材料から独立して答えられる問いはまとめ、新しい根拠が必要な評価は取得後に行う。候補が一意ならRouterを省略する方針は維持する。

### 評価について決まったことと保留事項

ユーザーが決めたのは、Jeviews自身を開発中の評価対象にし、教師役のAstraとFableのレビュー結果と突き合わせるという方針である。具体的な進め方は、実装が進んでからユーザーが指示する。
比較を成立させるため、同じコードの版・対象範囲で独立にレビューし、指摘箇所と根拠を比べる案を提示した。教師の一致も正解候補として根拠を確認する。この運用の細部はまだ固定しない。
現時点で精度や費用の測定結果はない。教師モデルの呼び出しや評価基盤の構築を実施済みと扱わない。

### 受領した設計レビューの追跡

ユーザーから9件の主な指摘と補足指摘を受領した。当初は「確認だけ」という依頼だったため、その時点では修正せず、その後に壁打ちを始めた。現在の状況は次のとおり。

| # | 指摘の要旨 | 現在の状況 |
| --- | --- | --- |
| 1 | 説明の裏付け不足によって、同じコードの `NG` が `explain` だけで `NEED_REVIEW` になる。説明状態とコード判定を分離し、`explanation_unverified` の扱いも見直す案 | 未決。本文の旧方針と型はレビュー対象のまま。説明が不十分な場合と、コードの判断根拠が不足する場合を分けて相談する |
| 2 | 既定 `scan` では材料不足が多くなり得る。既定を `investigate` にする案と、未解決割合を測る提案 | 未決。既定Modeの変更は合意していない |
| 3 | 追加調査の取得手段が未定義で、実装規模を見積もれない | 方針決定。言語を問わない読み取りと検索から始め、言語サーバーを後から追加する。5種類の候補案と詳細な取得契約は区別する |
| 4 | 初版の観点と選定基準が未定義 | 初版の5観点を決定。39項目すべて、または10〜15項目を必須とする案は採用していない。各観点の細かな質問は今後詰める |
| 5 | `Evidence.content` の保存で対象由来の秘密を複製する。Stateの肥大化と全体書き換えも懸念 | 未決。内容の常時保存をやめて再読込・ハッシュ照合する案、保存範囲・権限・秘密検出時の扱いを定義する案を受領 |
| 6 | `NEED_REVIEW` が未調査・材料不足・上限到達などを兼ねる。`partial` と終了コードの関係も曖昧 | 未決。上位の結果構造で調査状況を分ける案、`partial` の終了コードを明示する案、設定不備を `2` にする案を受領 |
| 7 | CI向けのbase/head差分指定がない。未追跡ファイルが `all` の対象外であることも伝わりにくい | 未決。`--base` 相当を初版に含めるかは未決定。言語非限定への変更と、Git上の対象範囲の変更を混同しない |
| 8 | 状態遷移と不変条件が未定義 | 未決。例としてdone時の判定、failed時の集計、説明とModeの関係を受領。`verdict: null` とエラーの関係はpending/runningも含めて検討する |
| 9 | 評価セットと数値での受け入れ基準が未定義 | 評価の方向性を決定。Jeviews自身とAstra・Fableを用いる。具体案・数値はユーザーの後続指示まで保留 |

補足指摘も未解決事項として残す。

- Jev、Flue、Ponytail、Decisions APIの用語定義を加える。
- 製品名 `Jeviews` とコマンド名 `jeview` の表記を統一する。
- 候補列挙の順序・IDを再現できるようにするか、候補を保存するかを決める。
- 反証のための調査が通常の追加調査回数に含まれることを明確にする。
- `problemScore` の尺度と意味を定義する。スコア、モデルの確信度、このツールで実測した精度を混同しない。離散ラベルを使う案も検討対象とする。
- リクエスト数やトークン数による上限と、事後の費用表示を分ける案を検討する。
- 対象コードに含まれる指示から、Jeviews自身のハーネスを守る方針を明文化する。
- 軸ごとの観点数が異なることを踏まえ、評価結果の見せ方を決める。

これらはレビューの受領記録であり、指摘の全内容を正しいと確定したものでも、すべて初版に実装すると決めたものでもない。

### 新しいチャットでの再開位置

取得方法・初版の観点・評価の進め方について、上記の合意から再開する。評価の細部を今すぐ決める質問は繰り返さない。
次に相談する候補は、レビュー#1の「コードの判定と説明の確認結果を分けるか」、#5の「対象コードをどこまで保存するか」、#6の「未調査・判断不能・実行失敗をどう返すか」である。ユーザーの指示に合わせ、一つずつ扱う。

再開時に渡す文章：

> Jeviews-design-v0.1.mdの冒頭「再開用メモ」を読んで、設計レビューの壁打ちを続けてください。初版は言語を限定しない読み取りと検索で小さく作り、言語サーバーは後から任意の拡張にします。入力の検証漏れ、エラーの握りつぶし、秘密情報の露出、フォーマット、Lint相当の5観点をJev自身に判断させます。慣習の推測は必要になるまで追加しません。開発中のJeviews自身を評価対象とし、AstraとFableを教師に結果を突き合わせます。評価の具体案は実装が進んでからこちらから指示します。未解決の指摘を、不明度の大きいものから一つずつ、平易な言葉で相談してください。

## 1\. 目的・決定事項・未決事項・実装順序

### 1\.1 目的

Jevの判断で調査範囲と深さを変え、コード上の根拠を伴う指摘を返すレビューCLIを設計する。

Jevは、問題の疑い、根拠の不足、追加調査の必要性を判断する。内部ハーネスは、許可された範囲から情報を取得し、応答を検証し、進捗と根拠を保存して、利用者へ確認可能な結果を返す。

この文書は、会話で合意したコアコンセプト、ライフサイクル、内部ハーネス、状態管理、出力契約を実装可能な形に整理した設計案である。既存CLIのコード変更や推論精度の検証を示すものではない。コード変更と動作検証は別工程とする。

### 1\.2 決定事項

- CLIとJSON出力を、人間・Git Hooks・CI・Agentの共通インターフェースとする。
- フォーマット・Lint相当の判断もJevが行い、調査過程・判定基準・根拠・未確認事項を確認できるOSSを目指す。
- 初版は言語を限定せず、読み取りと文字列・ファイル名検索で始める。言語サーバーは後から任意の拡張機能として追加する。
- 初版の観点は、入力の検証漏れ、エラーの握りつぶし、秘密情報の露出、コードのフォーマット、Lint相当の5つとする。
- 対象と調査方針を分離し、`diff`・`staged`・`all`、`scan`・`investigate`・`explain` の3 Modeを持つ。対象省略時は `diff` とする。
- 既定Modeは未確定。`scan` 案に対し `investigate` を既定にするレビュー提案を受領している。
- 追加調査は対象リポジトリ内に限定し、外部Web検索や資料取得は行わない。
- 根拠が確認できない場合は `NEED_REVIEW` とする。
- `GOOD` は安全性や完全性の保証ではなく、確認範囲内で問題が見つからなかったことを表す。
- 軸ごとの評価を保持し、別の軸の良い評価で問題や未確認事項を打ち消さない。
- 対象ごとの進捗を正本とし、実行全体の状態はそこから集計する。
- 対象単位の失敗と、実行全体を成立させられない致命的失敗を分ける。
- Stateと公開用結果を別の型にする。
- コアは通常のTypeScript関数とデータで構成し、実行基盤にFlueを採用する。哲学ごとのエージェント、継承階層、汎用プラグイン機構、独自の汎用Agent基盤は作らない。
- Provider AdapterがJevのDecisions APIを呼ぶ。レビューの型を会話や疑似ストリームの形式に固定しない。Flueへ接続する際に必要な形式の整形は実行層に閉じ、過剰な変換が必要ならFlueを外す判断材料にする。
- 追加調査は有限回数で制限し、同じ実行内の重複取得を防ぐ。
- 説明は対象ごとに1回生成し、Judgeで1回照合する。自動修正・再生成・説明から追加調査への差し戻しは初版で行わない。
- CLIはstdoutへバージョン付きJSONを1つ返し、進捗・診断情報はstderrへ出す。
- CIの成否は呼び出し側がJSONを読んで決める。
- 最初にFlue＋Jevの最小接続と調査を実装する。以降の判断基準・Mode・配布は共通処理を育てながら追加し、再開・並列は必要になった段階で具体化する。

### 1\.3 未決事項

未決事項は、実装開始前に確定すべき項目と、初版の実装後に評価して決められる項目に分ける。各項目には、決定期限、評価方法、暫定値、決定責任を定める。
以下の期限・数値・役割は実装案であり、担当者の割り当てではない。最新の合意として、初版の観点は5つに決まり、評価はJeviews自身の開発と並行して行う。評価の詳細と数値の合格基準は、実装が進んだ段階のユーザー指示で具体化する。

#### 実装開始前に確定すべき項目

|未決事項                   |決定期限             |評価方法                                         |暫定値                                       |決定責任          |
|-----------------------|-----------------|---------------------------------------------|------------------------------------------|--------------|
|初版の5観点の具体的な確認項目       |各観点を実装するとき      |開発中のJeviewsに対する評価を通して確認する。具体案は後続指示で決める             |入力の検証漏れ、エラーの握りつぶし、秘密情報の露出、フォーマット、Lint相当|レビュー設計責任者     |
|各確認項目の適用条件、質問、判定方針、質問の版|各項目を実装する前        |項目ごとの入力、期待判定、根拠要件、対象外条件をテストケース化する            |採用項目ごとに明示的な適用条件と質問版を持つ                    |レビュー設計責任者     |
|問題スコアと材料不足スコアの閾値       |一次評価の実装前         |既知の正常・不具合ケースで、見逃しと誤検知を比較し、問題と材料不足を別々に評価する    |既存実装の0.35・0.65は固定せず、評価後に決定                |レビュー設計責任者と評価担当|
|同時実行数                  |並列処理の実装前         |代表的なリポジトリで処理時間、API制限、失敗率、費用を比較する             |4                                         |実装責任者と運用責任者   |
|対象ごとの追加調査回数            |`investigate` 実装前|既知ケースで、追加調査による判定改善と費用・時間の増加を比較する             |最大3周                                      |レビュー設計責任者     |
|実行全体の時間制限              |CLI実行制御の実装前      |小規模・中規模・大規模リポジトリで完了率とタイムアウト率を測定する            |未設定または環境別設定                               |運用責任者         |
|費用上限の既定値               |モデル通信を実装する前      |代表的な入力で利用量と費用を測定し、上限到達時の結果を確認する              |既定値は未確定。設定可能にする                           |運用責任者         |
|保存ディレクトリの既定値           |保存処理の実装前         |CI、ローカル実行、権限不足、並列実行、再開の各環境で検証する              |CLI管理のローカル実行ディレクトリ。具体的なパスは未確定             |実装責任者と運用責任者   |
|既存CLIへの具体的な接続方法        |既存CLI変更の着手前      |既存の入力、出力、設定、エラー処理との互換性を確認する                  |既存CLIの境界を調査後に決定                           |既存CLI責任者と実装責任者|
|終了コードの最終仕様             |CLI契約を固定する前      |パイプ利用、CI、SIGINT、部分結果、設定失敗を含む統合テストを行う         |`0`：処理完了、`1`：設定・取得・APIなどの失敗、`130`：SIGINT  |CLI契約責任者      |
|実行時に利用するモデル            |モデル通信の実装前        |品質、利用量、応答形式、タイムアウト、利用可能なAPIを比較する             |モデル名は設定で固定し、実行時に保存する                      |モデル運用責任者      |
|利用量を取得できない場合の扱い        |費用集計の実装前         |利用量不明時に、費用表示、上限判定、再開、CI利用が破綻しないことを確認する       |利用量と費用を`null`とし、0として扱わない                  |運用責任者         |
|入力上限の決定方法              |入力分割処理の実装前       |利用するプロバイダーの実際の制限と、代表的な大規模入力で検証する             |30万トークンや24KBは共通上限として固定しない。プロバイダー制限に基づき設定する|実装責任者とモデル運用責任者|

#### 後回しにできる項目

|未決事項             |決定期限             |評価方法                                 |暫定値                         |決定責任     |
|-----------------|-----------------|-------------------------------------|----------------------------|---------|
|初版で採用しない確認項目の追加採用|初版の評価完了後         |既知ケースと実リポジトリで、追加項目の有効性、重複、費用、誤検知を評価する|初版では無効化し、確認範囲に理由を記録する       |レビュー設計責任者|
|確認項目の質問・判定方針の改善  |初版の評価サイクルごと      |検出漏れ、誤検知、根拠の妥当性、説明の裏付けを比較する          |質問版を更新し、`policyHash`で実行を分離する|レビュー設計責任者|
|閾値の継続的な調整        |初版の評価後           |評価セットを拡張し、精度と費用の変化を追跡する              |初版確定値を基準に、変更時は版を更新する        |レビュー設計責任者|
|同時実行数の環境別最適化     |並列処理の初版実装後       |CI、開発者端末、API制限下で処理時間・失敗率・費用を比較する     |設定可能な値として提供し、既定値は4から開始      |運用責任者    |
|追加調査回数の環境別最適化    |`investigate` 評価後|判定改善と追加費用・時間を比較する                    |既定値は最大3周から開始                |レビュー設計責任者|
|時間制限の環境別既定値      |初版運用後            |実行時間分布とタイムアウト後の未解決事項を分析する            |設定可能にし、初版では固定しない            |運用責任者    |
|費用上限の環境別既定値      |利用量の実測後          |利用量、費用、判定品質、上限到達率を比較する               |設定可能にし、初版では固定しない            |運用責任者    |
|保存ディレクトリの利用環境別既定値|初版配布前            |OS、CI、権限、複数実行の衝突を検証する                |CLI管理のローカル実行ディレクトリ          |実装責任者    |
|既存CLIとの統合方式の改善   |初版統合後            |利用者の移行負担、互換性、エラーの追跡性を評価する            |初版で決めた境界を維持し、必要な変更だけ追加する    |既存CLI責任者 |
|終了コードの追加オプション    |初版運用後            |JSONを読めない利用環境やCI要件の有無を調査する           |追加しない                       |CLI契約責任者 |
|モデルの切り替え方式       |初版運用後            |品質、費用、応答形式、再現性を比較する                  |設定でモデルを指定し、実行ごとに識別子を保存する    |モデル運用責任者 |
|利用量不明時の費用推定      |初版運用後            |推定値が意思決定を誤らせないか、実費との差を評価する           |推定せず`null`を返す               |運用責任者    |

30万トークンや既存サンプルの24KBを、新エンジンの共通上限として根拠なく固定しない。入力上限はプロバイダーの実際の制限を確認して決める。

### 1\.4 実装順序

2026-09-19追記：まずFlue＋Jevで最小の候補選択・取得・判定を動かす。以下は従来の段階案であり、再開・並列を初版の必須工程にしない。内部の汎用説明モデルを新たに導入せず、`explain` の具体的な提供方法は外側のCoding Agentとの役割分担に合わせて決める。

1. **判断基準・共通State・scan**

- 哲学と確認項目をデータで定義する。
- 対象登録、進捗、根拠の版、軸別JSON結果を整える。
- 共通処理から一次評価を実行する。

2. **investigate**

- 取得候補の列挙、Router/Judge、追加調査の終了条件を実装する。

3. **再開と並列処理**

- 保存・再開、並列数制御、同一実行内の重複取得防止を検証する。

4. **explain**

- 説明の1回生成と、説明・根拠の1回照合を追加する。

5. **配布とSkill**

- CLIのインストール方法と出力契約を固定し、`SKILL.md` を提供する。

独立した3つのレビューエンジンは作らず、各段階で共通処理を拡張する。
最初の観点は合意した5つに絞る。言語サーバー連携を必須工程にせず、読み取りと検索によるレビューを動かしてから必要な拡張を判断する。Jeviews自身への評価も開発と並行して進める。

## 2\. コアコンセプト

Jevの判断で調査範囲と深さを変え、コード上の根拠を伴う指摘を返すレビューCLIを目指す。

- **つなぎやすい**：CLIとJSON出力で、人間・Git Hooks・CI・Agentから共通して使える。
- **繰り返しやすい**：Jevを判断の中心に置き、こまめなレビューの費用を抑える。
- **確かめやすい**：指摘の根拠、未確認事項、実行コストを見える形で返す。

コードのフォーマットやLint相当の判断もJevが担当する。既存のFormatter・Linterを実行して結果を取り込む仕組みは、初版の方式に含めない。調査過程と判定基準を利用者が確認・改良できるOSSを目指す。

将来はCLIと `SKILL.md` を提供する。CLIが実行と状態管理を担い、Skillは呼び出し方、Modeの選び方、結果の扱い方をAgentに伝える。

## 3\. 内部ハーネスの設計方針

### 3\.1 7つの哲学

内部ハーネスとは、判断基準、Jevへ渡す情報、追加調査、進捗の記録、結果の返し方をつなぐJeviews内部の仕組みである。

全体に共通する哲学は「根拠がなければ、わからないと言う」である。問題があるという判断にも、確認した範囲で問題が見つからなかったという判断にも、必要な材料が揃っているかを確認する。

|ID|哲学          |大切にすること                   |対応する観点候補       |
|--|------------|--------------------------|---------------|
|A |約束どおりに動く    |仕様に沿った結果を返し、既存の使い方を守る     |1〜6、11、12、22、32|
|B |人と情報を守る     |許可された操作だけを行い、大切な情報を漏らさない  |14〜19、39       |
|C |失敗を扱える      |失敗や中断が起きても、状況を把握して回復できる   |7〜10、13、20、21  |
|D |資源を無駄にしない   |時間・メモリ・通信・料金を必要な範囲に収める    |23、24          |
|E |理解しやすく、変えやすい|役割を整理し、必要な変更を少ない負担で行える    |27〜31、33       |
|F |根拠を確かめられる   |テスト・記録・説明から、動作や判断の理由を確認できる|25、26、34、37、38 |
|G |使う人が迷わない    |操作方法や結果、次に取る行動がわかる        |35、36          |

哲学は「何を大切にするか」、確認項目は「何を確かめるか」、Modeは「どこまで調べ、説明するか」を表す。

7つの哲学を内部の整理軸として持つ。不具合・セキュリティの確認は必要な機能とする。39項目は候補一覧として維持し、初版は入力の検証漏れ、エラーの握りつぶし、秘密情報の露出、フォーマット、Lint相当の5観点から始める。細かなルール、適用条件、質問、閾値は具体化と評価を経て決める。
周囲のコードから書き方の慣習を推測する指示は、初版のプロンプトに含めない。必要になった段階で検討する。

SOLIDなどの原則は、変更のしやすさを考える手がかりにする。指摘には、実際にどの変更が難しくなるかなど、具体的な影響を伴わせる。

### 3\.2 責務の境界

- **判断基準のデータ**
  - 哲学のID
  - 確認項目のID
  - 適用条件
  - Jevへの質問
  - 判定方針
  - 質問の版
- **共通のレビュー処理**
  - Modeと判断基準に従って評価、追加調査、説明を進める。
  - 対象の状態を更新する。
  - レビューを続けるか終了するかを判断する。
- **入出力の処理**
  - リポジトリの読み取り
  - モデルの呼び出し
  - 状態保存
  - データまたはエラーの返却
- **結果を組み立てる処理**
  - 軸別の判定
  - 指摘
  - 根拠
  - 不明事項
  - 実行状況
  - 費用
    を公開用JSONへまとめる。

これらは責務の境界を表す。通常の関数とデータで実装し、哲学ごとのエージェント、継承階層、汎用プラグイン機構を初版の前提にしない。

必要な依存は関数の引数で渡せるようにする。例えばレビュー処理へ、読み取り、Jevへの問い合わせ、保存の関数を渡す。実装が1種類でも、責務分離に必要な境界は設ける。

実行途中のStateと、利用者へ返す結果は別の型にする。途中状態をそのまま公開仕様にしない。

### 3\.3 Jevとハーネスの分担

Jevは次を判断する。

- 問題の疑い
- 根拠の不足
- 追加で確認する必要性
- 候補が複数ある場合の取得先
- 追加情報による反証の必要性
- 説明と根拠の整合性

ハーネスは次を管理する。

- 実在する取得候補の用意
- 許可範囲の確認
- 情報の読み込み
- 応答の検証
- 進捗の保存
- 調査回数の上限
- 並列数、費用、終了条件

モデルの選択肢とスコアは、コード上の候補・根拠へ対応付ける。Jevが自由文で理由やファイル名を生成することを前提にしない。

同じ情報で答えられる独立した問いはまとめて問い合わせる。軸の数とモデルの呼び出し回数を一致させる必要はない。

## 4\. 対象とMode

対象と調査方針を独立させる。

```bash
jeview
jeview diff --mode scan
jeview staged --mode scan
jeview all --mode investigate
jeview all --mode explain
```

|対象|初版の範囲|
|---|---|
|`diff`（既定）|インデックスと作業ツリーの差分。未ステージの変更|
|`staged`|HEADとインデックスの差分。ステージ済みの変更|
|`all`|Git管理下の対象コードファイルの作業ツリー内容。言語は限定しない|

`all` は未追跡ファイルを自動で含めない。生成物などの除外と、読み取り失敗を区別して報告する。

「全体」は、指定した対象をすべて一次評価して確認範囲を報告する意味であり、すべての不具合を発見する保証ではない。

大きなDiff・ファイルは、パス、行範囲、差分の対応を保持して分割する。ファイル名、宣言、必要な周辺コードを付け、単純なバイト切断を避ける。

追加調査は対象リポジトリ内のコード、テスト、設定、ドキュメントに閉じる。言語を問わず、その判断に必要な資料を参照候補に含める。初版はファイルの読み取りと文字列・ファイル名検索を使い、言語サーバー連携は後から任意の拡張として追加する。外部のWeb検索や資料取得は行わない。外部資料がなければ判断できない場合は、不足する情報を残す。

取得処理はリポジトリの境界を検証し、パス指定やリンク経由でも範囲外を読まない。参照した版を根拠とともに保存する。

|Mode         |通るSTEP                   |出力の範囲                      |
|-------------|-------------------------|---------------------------|
|`scan`       |1 → 2 → 3 → 6            |観点別スコア、判定、追加確認の必要性         |
|`investigate`|1 → 2 → 3 → 必要なら4 → 6    |上記に加え、問題の分類、根拠の参照、未解決事項    |
|`explain`    |1 → 2 → 3 → 必要なら4 → 5 → 6|上記に加え、問題の場所、発生条件、影響、修正方針の説明|

共通処理にModeの方針を渡し、3つの独立したレビュー処理を作らない。

## 5\. ライフサイクル

|STEP  |処理                          |主な保存内容                  |
|------|----------------------------|------------------------|
|1 開始  |対象、Mode、有効な確認項目、調査上限を確定     |実行ID、設定、判断基準とコードの版      |
|2 対象登録|列挙、除外、分割                    |対象一覧、未確認キュー、除外範囲        |
|3 一次評価|哲学に対応する確認項目で、問題の疑いと材料不足を評価  |観点別スコア、追加調査の必要性、次に確認する候補|
|4 追加調査|必要ならRouterで選択し、取得してJudgeで再評価|取得済み範囲、根拠、未解決事項         |
|5 説明  |1回生成し、Judgeで1回照合            |説明文、対応する根拠、照合状態         |
|6 終了  |全対象の結果を集約                   |軸別・対象別の判定、確認範囲、未確認事項、費用 |

STEP 3〜5は対象ごとに進む。異なる対象を並列処理できるため、実行全体に「現在のSTEP」を1個だけ置かない。

一つの対象の読み取りやAPI呼び出しが失敗しても、失敗を記録して残りの対象は続行する。実行全体を成立させられない設定不備・保存不能などは、対象単位の失敗と分けて扱う。

### 5\.1 追加調査の分岐

1. Jevが判断材料の不足と追加調査の必要性を判定する。
2. ハーネスがリポジトリ内の取得候補と残りの上限を確認する。
3. 取得先が一意なら直接取得する。複数候補から選択する必要がある場合だけRouterを呼ぶ。
4. 候補がなければ未解決として終了する。
5. 問題が疑われる場合は、問題を防ぐ処理や呼び出し条件などの反証も確認する。
6. 材料が揃い評価が済んだら、対象の判定と根拠を保存する。
7. 材料不足のままJevが調査終了を選んだ場合も、不足を残して `NEED_REVIEW` にする。
8. 調査上限、新情報なし、必要なコードの取得不能に達した場合は、未解決事項を残して `NEED_REVIEW` にする。
9. `scan` では、追加調査が必要でも取得を行わず `NEED_REVIEW` にする。

### 5\.2 説明の扱い

判定への反映方法はレビュー#1で再検討中である。以下は従来案として残し、最終契約として確定しない。

説明は対象ごとに1回生成し、Judgeで1回照合する。裏付け不足の説明は `unverified` として残す。

コード自体の評価と説明の照合状態は分けて保持する。結果を組み立てる処理が、説明の裏付け不足を対象の最終判定へ反映し、必要に応じて `NEED_REVIEW` にする。

裏付け不足の説明は確定的な指摘として表示しない。初版では説明の自動修正・再生成や、説明から追加調査への差し戻しを行わない。

説明を必要とする指摘候補がない場合は生成を省略する。材料不足の対象には不足事項を返し、具体的な問題を作らない。

通信エラーの有限な再試行と、完成した説明を作り直す処理は区別する。Jevによる照合は正しさの保証ではない。

## 6\. 役割

|担当               |責務                                            |
|-----------------|----------------------------------------------|
|Router Jev       |複数候補から選択が必要な場合に、調査先・観点を選ぶ                     |
|Judge Jev        |各確認項目に対する問題の疑い、根拠の充足、追加調査の必要性、反証、説明との整合性を評価する |
|内部ハーネスの共通処理      |Jeviewsの判断基準・Mode・上限・状態遷移をFlueの実行層から利用し、取得・評価・保存をつなぐ|
|読み取り・モデル通信・保存の各処理|各入出力を担当し、結果またはエラーを返す。処理の進め方は共通処理に任せる          |
|外側のCoding Agent     |根拠と未確認事項を受け取り、広い文脈での調査・説明・修正を担当する。内部に汎用モデルを追加しない|

自由なファイル名や存在しない行番号をモデルの回答からそのまま採用しない。候補IDを取得処理で実在するパス・範囲へ対応付ける。

`investigate` では既知の問題分類とコード参照を組み合わせる。スコアだけから具体的な原因を作らない。分類候補が合わなければ未解決に残す。

コアはFlueに依存させず、通常のTypeScript関数と型で判断基準・状態遷移・有限の終了条件を表現する。Flueの実行層がそれらを利用して処理を進める。Jevの通信はProvider Adapterが担当し、タイムアウト、キャンセル、応答検証、利用量の記録を残す。Flueと別の調査ループを同時に動かして進捗を二重管理しない。

## 7\. 判定と結果の扱い

各軸について、適用した確認項目、判定、根拠、不明事項を返す。根拠を確認できない場合は `NEED_REVIEW` とする。

適用対象外の項目と、有効になっていない項目は、確認済みの `GOOD` に含めない。それぞれ理由とともに確認範囲へ記録する。

各軸の点数を平均して総合点にしない。別の軸の良い評価で、問題や未確認事項を打ち消さない。

一つの対象に確定した指摘と不明事項が混在しても、両方を残す。最終判定が `NEED_REVIEW` になっても、確認できた指摘や各軸の評価を消さない。

## 8\. 保存・再開・重複取得の防止

レビュー#5で、対象コードの内容を保存する必要性、秘密情報の複製、Stateの容量について再検討中である。以下の保存方式と `Evidence.content` は確定仕様ではない。

旧案ではCLIが管理するローカルの実行ディレクトリへ、JSONのStateと必要な根拠を保存する想定だった。Flue採用後はStateの正本を一つにし、Flueの保存との対応を実装時に確認する。以下は引き続き検討する保存の要件であり、JSONとFlueへの二重保存を必須にするものではない。保存場所は設定可能にし、APIキーなどの認証情報はStateに含めない。

- 対象登録後、および取得・評価・説明の完了ごとに保存する。
- 一時ファイルへ書き込んだ後に置換し、途中書き込みでStateを壊さない。
- 並列タスクからの状態更新は1つの書き込み処理に集約する。
- 起動時には保存した状態を読み、未完了の処理だけを再実行する。
- 再開時にコードや設定が変わっていたら、以前の結果へそのまま混ぜない。初版は版の不一致を通知して新しい実行を求める。
- 行番号だけを保存せず、読んだ内容の版と組み合わせる。根拠の内容を再現できるよう、必要範囲を保持する。
- `staged` の追加調査はステージ済みの版を基本とする。作業ツリーの未ステージ変更を無言で混ぜない。`diff` と `all` は作業ツリーを基準にする。

実行をまたぐ判定キャッシュと、その無効化処理は初版では実装しない。新しい実行では改めて評価する。

同じ実行内では、パス、コードの版、行範囲をキーに取得済みコードの読み直しを防ぐ。必要なら単純な `Map` を使い、汎用キャッシュ層は作らない。

保存済みの同じ実行を再開する場合は、コードと設定の一致を確認したうえで完了した処理を再利用する。これは新しい実行への判定キャッシュとは別の機能である。

## 9\. 調査量と並列実行

追加調査を続ける必要があるかはJevに判断させる。CLIには対象ごとの有限な調査回数上限を持たせ、Jevの判断でその上限を変更しない。

同時実行数4、追加調査最大3周は比較評価用の仮案であり、確定値ではない。上限値と全体の時間・費用制限の既定値は未確定とする。

1周は「必要なら取得先を選択 → 取得 → 再評価」である。Routerを省略しても1周に数える。同じ内容しか増えない場合は、上限前でも止める。

HTTP再試行も回数と実行全体の上限に含める。実費が事前に確定しないため、費用上限は厳密な課金保証として扱わない。

独立した質問は同じ入力でまとめる。別の回答や新しい取得結果に依存する問いは、次の呼び出しへ分ける。

## 10\. CLIの出力契約案

レビュー#6・#7を受け、調査状況と終了コードの対応、CI向けの差分指定を今後具体化する。以下は現在の案である。

- `stdout`：バージョン付きJSONを1つ返す。判定、実行状態、対象別と哲学別の結果、確認範囲、費用を含める。
- `stderr`：進捗・診断情報を出す。JSONをパイプで読む利用者の入力へ混ぜない。
- 終了コード `0`：レビュー処理が完了。`NG` や `NEED_REVIEW` を含み得る。
- 終了コード `1`：設定・取得・APIなどの失敗。部分結果があれば、実行状態とともにJSONで返す。
- 中断：Stateを保存して終了する。SIGINTでは `130` を使用する案とする。
- CIの成否は呼び出し側がJSONを読んで決める。専用の判定別終了オプションは初版にも必須の開発予定にも含めない。
- 費用にはRouter、Judge、説明モデル、再試行を含める。取得できない利用量は `null` とし、0として表示しない。

Skillはこの契約を利用する。`GOOD` を安全性の保証として扱わず、`NEED_REVIEW` の不足情報と未確認範囲を読んで追加確認する。

## 11\. 最初に検証する条件

開発中のJeviews自身を最初の評価対象にし、AstraとFableを教師役にレビュー結果を突き合わせる。具体的な評価手順、評価セット、数値の合格基準は、実装が進んでからユーザーが指示する。以下は確認したい性質の一覧であり、数値のリリース条件を決定したものではない。

- 材料不足を低リスクと取り違えて `GOOD` にしない。
- 哲学別の問題と不明事項を保持し、他の軸の点数で打ち消さない。
- 適用対象外・無効な確認項目を、確認済みの項目に数えない。
- Jevが追加調査を必要と判断しても、リポジトリの境界と回数上限を越えない。
- 追加情報で疑いが解消する例と、問題を裏付ける例を区別できる。
- 新情報なし・上限到達でループが終了し、未解決事項が残る。
- 取得先が一意ならRouterを省略でき、複数候補の場合だけ選択を行う。
- 説明の裏付け不足を返し、自動再生成や追加調査のループへ入らない。
- 対象別の進捗から実行全体の結果を集計できる。
- API失敗とレビュー上の要確認を区別できる。
- 一部の対象が失敗しても残りの対象を処理し、失敗した対象を `GOOD` にしない。
- 中断・再開で確認済み対象を失わず、異なるコードの版を混ぜない。
- 根拠のパス・行範囲が対象の版に一致し、存在しない参照を出力しない。
- JSONを別プログラムから読める。進捗がstdoutに混ざらない。

既知の不具合と正常コードを同じ評価セットで比較し、見逃し、誤検知、問題箇所、説明の裏付けを測る。処理時間、費用、確認範囲も記録し、性能改善で精度や確認範囲が変わっていないか確かめる。

## 12\. Ponytailレビューで削減した7件

この表は2026-09-18時点の履歴である。#1のFlue見送りは2026-09-19に変更し、実行層にFlueを採用して問題があれば外す方針とした。コアの独立性は維持する。

|#|削除・縮小した仕組み                       |置き換え・残すもの                          |
|-|---------------------------------|-----------------------------------|
|1|Flue依存と、決定結果の会話・疑似ストリーム変換        |通常のasync関数によるJev直接呼び出し。タイムアウト・検証は維持|
|2|追加取得ごとのRouter呼び出し                |一意の取得先は直接取得。選択が必要なときだけRouter       |
|3|実行全体の根拠台帳と多段ID参照                 |対象ごとの根拠配列。対象内の参照で追跡を維持             |
|4|実行をまたぐ判定キャッシュと無効化管理              |同一実行の重複取得防止と、同じ実行の再開               |
|5|説明の自動修正・再生成・追加調査への差し戻し           |1回生成・1回照合。未確認は明示して返す               |
|6|全体のcompleted / partialと対象別進捗の二重更新|対象別進捗から集計。開始・中断・致命的失敗は記録           |
|7|CI判定別の終了オプションの必須化                |JSONを受け取った呼び出し元が成否を決定              |

現在は `diff / staged / all`、3つのMode、判断材料が足りない場合の追加調査、根拠の記録を扱う。保存・再開の詳細はFlueとの対応を含めて具体化する。

この変更は設計への反映であり、既存CLIのコードを削除済みという意味ではない。

## 13\. Stateの最小構造案

実行設定と対象ごとの処理状態を保持し、根拠は各対象の中に置く。実行全体の根拠台帳は作らない。モデルへは現在の判断に必要なコードだけを渡す。

対象内の根拠配列は追記のみとし、評価・説明はその配列インデックスを参照する。根拠を共有する必要が実際に生じた場合だけ、共有IDを導入する。

```ts
type Verdict = 'GOOD' | 'NG' | 'NEED_REVIEW';
type Mode = 'scan' | 'investigate' | 'explain';
type AxisId = 'A' | 'B' | 'C' | 'D' | 'E' | 'F' | 'G';

// 出力時に算出する値。RunStateには重複して保存しない。
type RunStatus =
  | 'created' | 'running' | 'completed'
  | 'partial' | 'interrupted' | 'failed';

type ItemPhase =
  | 'pending' | 'assessing' | 'investigating'
  | 'explaining' | 'done' | 'failed';

type StopReason =
  | 'evaluated' | 'scan_only' | 'budget_limit'
  | 'no_new_evidence' | 'unavailable_context'
  | 'explanation_unverified' | 'api_error';

interface Evidence {
  path: string;
  revision: string; // 実際に読んだ内容のハッシュ
  startLine: number;
  endLine: number;
  origin: 'index' | 'worktree';
  role: 'target' | 'context';
  content: string; // 再開・根拠確認に必要な範囲
}

interface Assessment {
  id: string;
  axisId: AxisId;
  checkId: string;
  problemScore: number;
  needsContextScore: number;
  evidenceIndexes: number[];
  model: string;
  questionVersion: string;
}

// 説明文を生成する前に、分類と根拠を対応付ける。
interface Finding {
  axisId: AxisId;
  checkId: string;
  category: string;
  evidenceIndexes: number[];
}

interface ReviewItem {
  id: string;
  phase: ItemPhase;
  inputHash: string;
  evidence: Evidence[];
  assessments: Assessment[];
  findings: Finding[];
  unresolved: Array<{
    axisId: AxisId;
    checkId: string;
    code: string;
    candidateIds: string[];
  }>;
  rounds: number;
  verdict: Verdict | null;
  stopReason: StopReason | null;
  explanation?: {
    text: string;
    evidenceIndexes: number[];
    status: 'draft' | 'supported' | 'unverified';
  };
  error?: { code: string; message: string };
}

interface RunState {
  schemaVersion: 1;
  id: string;
  execution: {
    startedAt: string | null;
    interruptedAt: string | null;
    fatalError?: { code: string; message: string };
  };
  scope: 'diff' | 'staged' | 'all';
  mode: Mode;
  snapshotId: string;
  policyHash: string;
  items: ReviewItem[];
  exclusions: Array<{ path: string; reason: string }>;
  usage: {
    requests: number;
    inputTokens: number | null;
    outputTokens: number | null;
    costUsd: number | null;
    complete: boolean;
  };
}
```

この型は状態の責務を示す最小案である。実行設定の実体、モデルと質問の識別子、利用量は再開と追跡に必要な範囲で保存する。汎用イベントストアへ拡張しない。

共通処理が、検証済みの評価とコード参照から `Finding` を組み立てる。説明モデルはこれを受け取り、判定や根拠を書き換えない。

Jevの追加調査に関する回答も、対象、確認項目、評価回と対応付けて保存する。自由文の推論過程を保存する必要はない。

保存する設定には、有効な確認項目、適用条件、判定方針、質問の版を含める。`policyHash` でその組み合わせを識別し、再開時に判断基準を無言で変更しない。

### 13\.1 状態と判定の意味

対象ごとの `phase` を進捗の正本にする。

対象登録が完了した実行では、全対象が `done` なら `completed`、処理が落ち着いた時点で対象の失敗が残れば `partial` と算出する。対象が0件なら処理対象なしを明示して `completed` とする。これらの集計値を対象の更新と並行して書き換えない。

実行自体の致命的失敗は `fatalError` から `failed`、未完了対象を残した中断は `interruptedAt` から `interrupted` と判断する。対象登録前・実行中の状態には開始記録を使う。

再開時は過去の中断記録を解除し、未完了の処理を進める。明示的な中断記録のない異常終了も、保存済みStateから未完了処理を再開する。

- `done` は対象の処理終了を表す。`NEED_REVIEW` と両立する。
- `completed` は列挙した全対象が方針に従って処理を終えた状態である。全対象が `GOOD` である必要はない。
- `partial` はAPI・取得処理などの失敗により、一部の対象を評価しきれなかった状態である。
- `interrupted` は中断され、再開可能な状態である。未確認キューを維持する。
- `failed` は初期設定や致命的なエラーで実行を成立させられなかった状態である。
- 推論できなかった対象を `GOOD` にしない。
- API失敗では原則 `verdict: null` とし、エラーを保存する。

調査で未解決の問題が残る場合は `NEED_REVIEW` とする。0\.35・0\.65などの閾値は、既存実装を引き継ぐ前提で固定せず、既知の正常・不具合ケースで決める。問題スコアと材料不足スコアを混同しない。

## 14\. 哲学に対応する39の観点候補

選定用の一覧である。各項目は対象の性質とリポジトリ内の根拠に応じて適用する。特に仕様や設計方針は、資料が見つからなかった場合にモデルの推測で補って断定しない。

1. **仕様との食い違い**：READMEや仕様書の動作と実装が一致しているか。
2. **計算・条件分岐の誤り**：計算式や条件の書き間違いで、誤った結果にならないか。
3. **境界の値への対応**：0件、上限ちょうど、負数、長い文字列などで壊れないか。
4. **値がない場合の対応**：未入力、null、検索結果なしなどを扱えるか。
5. **入力の検証**：不正な形式や範囲外の値を処理前に確認しているか。
6. **状態の切り替え**：完了後に処理中へ戻るなど、不自然な状態にならないか。
7. **エラーの扱い**：失敗を握りつぶしたり、成功として返したりしていないか。
8. **途中で失敗した場合の復旧**：一部だけ処理された状態を把握し、再開や取り消しができるか。
9. **同時実行による不具合**：複数の処理による更新で、データや結果を壊さないか。
10. **繰り返し実行への対応**：再送や再試行で、登録・請求・通知などが重複しないか。
11. **データ更新の整合性**：関連するデータの片方だけが更新され、矛盾が残らないか。
12. **日時・数値・文字の扱い**：時差、丸め誤差、日本語などを正しく扱えるか。
13. **後片付けと中断処理**：接続、ファイル、タイマーなどを使い終えたら解放できるか。
14. **本人確認**：ログインが必要な処理を、未ログインのまま実行できないか。
15. **操作権限**：他人のデータや管理者向け機能に、権限なくアクセスできないか。
16. **入力を通じた不正な操作**：入力がSQL・コマンド・画面上のスクリプトとして実行されないか。
17. **ファイル・URLの参照範囲**：指定された参照先から、本来読ませない場所へアクセスできないか。
18. **秘密情報の漏えい**：APIキーやパスワードが、コード・ログ・出力に含まれていないか。
19. **個人情報の扱い**：必要以上の個人情報を返したり、記録したりしていないか。
20. **設定ミスへの対応**：必要な設定がない場合や不正な場合に、理由を示して止まれるか。
21. **外部サービスの障害への対応**：遅延、失敗、想定外の応答を扱えるか。確認にはリポジトリ内の実装・資料を使う。
22. **既存の利用者への影響**：API、CLI、保存データの変更で、既存の使い方を壊さないか。
23. **処理速度**：同じ検索や読み込みを繰り返すなど、不要な処理が増えていないか。
24. **メモリ・通信量・料金の上限**：大量の入力や繰り返し処理で、消費が際限なく増えないか。
25. **テストの有効性**：重要な動作や失敗時の動作を確認でき、実装が壊れたら検出できる内容か。
26. **問題の調べやすさ**：障害時に、どの処理が何の理由で失敗したか追えるか。
27. **役割のまとまり**：一つの関数や部品に、関係の薄い仕事が集まっていないか。
28. **部品同士の依存**：一つの機能の変更で、無関係な部品まで修正する必要がないか。
29. **過剰な仕組み**：現在必要のない共通化・設定・階層が、理解や変更を難しくしていないか。
30. **重複による修正漏れ**：同じルールが複数箇所にあり、片方だけ直される構造になっていないか。
31. **コードの読みやすさ**：名前や処理の流れから、目的と動作を理解できるか。
32. **型とデータ構造**：扱える値が明確で、不正な状態を簡単に作れてしまわないか。
33. **リポジトリの方針との一致**：明文化された設計方針や開発ルールに沿っているか。
34. **説明と実装の一致**：コメント、使用例、CLIのヘルプが現在の実装と一致しているか。
35. **利用者への伝わりやすさ**：操作方法、進捗、エラー、次に取るべき行動がわかるか。
36. **画面の操作しやすさ**：キーボード操作や読み上げなどでも、必要な操作ができる作りか。
37. **AIへ渡す情報の十分さ**：判断に必要な情報が抜けたり、途中で切れたりしていないか。
38. **AIの結果の扱い**：根拠のない回答や形式の崩れた出力を、確定結果として使っていないか。
39. **AIへの指示の混入**：レビュー対象に含まれる命令を、ツール自身への指示として扱っていないか。

この一覧を採用しても、テストの実行や画面の実操作が自動で追加されるわけではない。現行のライフサイクルはリポジトリ内の情報を読み取って評価する設計であり、実行して確かめた結果とは区別する。
