Review every file in this directory (use git ls-files to enumerate; read each file fully). Do not modify anything.
For each file, decide for each of the questions below whether the answer is clearly YES for that file. Report only YES cases, one per line, exactly in this format:
<file path> | <question id> | <line number> | <one sentence of evidence>
Questions (`content` means the file being reviewed):
- input_unchecked_use (applies to: code): Does `content` take a value read from outside the program (command-line arguments, environment variables, network responses, or file contents) and use it as a number, a URL, a file path, or a member of a fixed set of options, without first checking that it has that form? A value that is only passed through as an opaque string, such as an API key or a model name, does not count.
- input_missing_unhandled (applies to: code): When `content` reads a value from outside the program (command-line arguments, environment variables, network responses, or file contents) and that value is absent or empty, does the code proceed as if the value were present?
- error_empty_catch (applies to: code): Does `content` contain a catch block or error handler that discards the error without logging it, rethrowing it, or returning a failure?
- error_success_after_failure (applies to: code): Does `content` return a success value or continue as if the operation succeeded after that operation has failed?
- error_unhandled_promise (applies to: code): Does `content` start an asynchronous operation (a promise, future, or async task) whose failure is never awaited or handled? Enqueuing a background job through a job queue (for example perform_async, perform_later, or enqueue) does not count.
- secret_hardcoded (applies to: code/test/config/doc/other): Does `content` contain a hard-coded credential value, such as an API key, password, access token, or private key? An empty value, a placeholder such as "changeme", "xxx", or "<your key>", or a variable name with no value after it does not count.
- secret_logged (applies to: code/test/config/doc/other): Does `content` write a credential value (API key, password, or token) to logs, standard output, standard error, or an error message?
- format_indentation (applies to: code/test/config/template): Does `content` mix indentation styles, such as tabs and spaces or different indent widths, within the file?
- format_quotes (applies to: code/test/config/template): Does `content` mix single and double quotes for string literals without a consistent rule? Using double quotes only where interpolation or escape sequences are needed, and single quotes elsewhere, is a consistent rule.
- format_spacing (applies to: code/test/config/template): Does `content` have inconsistent spacing around operators, commas, or braces within the file?
- lint_unused_import (applies to: code): Is there an import, require, or use statement in `content` that brings in a name which is never referenced anywhere else in `content`? A require or import whose purpose is to load a library or plugin for its side effects (for example require "rails" or a railtie) does not count.
- lint_unused_variable (applies to: code): Is there a local variable assigned in `content` (for example with const, let, var, or a plain assignment) whose name is never read afterwards?
- lint_unused_param (applies to: code): Is there a function or method parameter in `content` that is never referenced inside that function or method body? A parameter whose name starts with an underscore does not count. A parameter that is passed on to another call, including super, counts as referenced.
- lint_unreachable (applies to: code): Does `content` contain code that can never execute, for example statements after a return or throw?
- lint_duplicate_condition (applies to: code): Does `content` repeat the same condition in an if/else chain or switch so that a later branch can never be reached?
- complexity_branchy_function (applies to: code): Does `content` contain a single function or method whose body has roughly 15 or more independent branch points (if, else if, loops, case, catch, ternaries, and && / || operators counted together), the kind a cyclomatic-complexity linter would flag?
- lint_constant_condition (applies to: code): Does `content` contain an if, unless, while, or ternary condition, or a comparison, that is always true or always false? A method or function that simply returns a literal true or false is not a condition and does not count.
Rules: be strict and literal about each question. Do not report style opinions outside the questions. Do not add headings, summaries, or files without findings. Output the lines only.